## reviser — round 1

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|------------------------------------------|
| design-auditor-r1-f1 | Fixed | Confirmed against the live advisory DB and by direct test: `fast-xml-parser` is critical across `<=5.6.0`, and `5.9.2` is the first fixed release — no 4.x version clears it. Verified `5.9.2` ships a dual CommonJS/ESM build (so the fork's `require('fast-xml-parser').XMLParser` still resolves) and that `new XMLParser().parse(body)[rootTag]` returns the identical default shape for a QBO response, so the major bump is version-only churn for this codebase. Rewrote Decision 4 to bump to `^5.9.2` with this rationale and a rejected "stay on 4.x with a residual advisory" alternative; replaced the "Adopting fast-xml-parser 5.x" Non-Goal with a "do not rewrite the XML code" Non-Goal; updated Current State, the deferred-decision entry, and the risk table accordingly. Success Criteria now claim a fully clean audit that the chosen version actually delivers. |
| design-auditor-r1-f2 | Fixed | Verified git history (`deb64fe`/`8de037b` Aug 2024 predate the `2.0.46` merge `8e070aa` Jun 2025; post-merge commit `4b95907` is the mocha bump). Reworded both the Problem Statement and Current State paragraphs to state the feature was committed before the most recent upstream sync and carried across the merge intact, with the mocha bump as the only post-merge local change — preserving the correct net delta while fixing the ordering. |
| design-auditor-r1-f3 | Fixed | Named the merge target explicitly as upstream tag `2.0.50` in the Migration Strategy, and added a step-0 precondition requiring the upstream tree and the `fuze` `qboApi.ts` construction to be confirmed (axios-based hookless `refreshAccessToken`, 10-param `index.d.ts` constructor, 11-arg positional call in `fuze`) before any merge, with an explicit stop-and-re-baseline instruction if upstream's shape differs. |
| design-auditor-r1-f4 | Fixed | Enumerated all eight issue-#3 advisories in Success Criteria as a table with source, what clears each, and expected end state (all cleared, none retained). Verification now asserts each named advisory is absent from `npm audit` output rather than running a bare unqualified audit. |
| design-auditor-r1-f5 | Fixed | Added a Risks row for "selected dependency versions do not clear all issue-#3 advisories" (Low/High) with the mitigation that versions were chosen against the live advisory DB and Verification asserts the named advisories are absent before landing. |
