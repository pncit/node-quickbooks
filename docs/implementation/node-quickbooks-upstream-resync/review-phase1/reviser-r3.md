## reviser — round 3

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| architect-r1-f1 | Fixed | Human ruling confirms the required tags already exist (`plan-approved` → `04c4d8c`, `design-approved` → `e9b1aee`), making both approval commits permanently reachable independent of the reflog. Updated `implementation-phase1-notes.md` §5 to document the commit-graph orphaning, the tag resolution, and the explicit decision that the fork's pre-plan code history (pre-`04c4d8c`) remains intentionally orphaned per the plan's Assumptions. Updated §11 to record the finding as resolved. The architect's prescribed falsifiable outcome (a durable, branch-independent ref) is met. |
| project-lead-r1-f1 | Fixed | Human ruling provided: "The required git tags have been created (plan-approved -> 04c4d8c, design-approved -> e9b1aee); there is no further audit risk on this finding." Tags verified on disk (`git rev-parse plan-approved` → `04c4d8cb…`, `git rev-parse design-approved` → `e9b1aee6…`). The project lead's recommended path — "create a lightweight tag so the approved-plan commit survives `git gc`" — is the path the human chose. Notes updated as above. |
| typescript-cop-r1-f1 | Rejected | Phase 1's scope is upstream-verbatim: the plan's Phase 1 steps explicitly prohibit any code edits to `index.d.ts` ("Any code edits to `index.js`, `index.d.ts`, or `package.json`" is listed under Explicitly Out-of-Scope). Narrowing `res?: any` to `unknown` would deviate from the upstream-verbatim deliverable and violate the Scope Lock. The plan's Non-Goal further states "no refactoring of fork code beyond the two feature regions" — `QuickBooksCallback<T>` is not in either feature region. The TypeScript cop's own recommendation is "apply this fix when the Phase 3 `.d.ts` edit lands"; that is the correct phase for this change. |
| typescript-cop-r1-f2 | Rejected | Phase 1's scope is upstream-verbatim (no `index.d.ts` code edits; see f1 rationale above). Additionally, the plan's Assumptions explicitly address the error-path argument shape: the reset "intentionally adopts upstream's status-based classification and `.catch` error-callback shape," and the plan Non-Goal states "No improvement to callback error propagation — the existing contract is preserved exactly." All three of the cop's proposed options (a)/(b)/(c) either change the declared type or the runtime observable behavior in ways the plan deliberately defers or precludes. The cop's own recommendation is "apply in Phase 3 alongside the other `.d.ts` edits." |
| typescript-cop-r1-f3 | Rejected | Phase 1's scope is upstream-verbatim (no `index.d.ts` code edits; see f1 rationale above). The `upload` method's `stream: any` parameter is outside both feature regions and any Phase 1 exit gate. The plan's Non-Goal prohibits refactoring beyond the two feature regions. The TypeScript cop's own recommendation is "apply in Phase 3 alongside the other `.d.ts` edits," which is the correct phase when `index.d.ts` is being modified for the constructor 11th-parameter addition. |
| typescript-cop-r1-f4 | Rejected | Phase 1's scope is upstream-verbatim (no `index.d.ts` code edits; see f1 rationale above). `createTaxService`'s `taxService: any` parameter is outside both feature regions and any Phase 1 exit gate. The plan's Non-Goal prohibits refactoring beyond the two feature regions. The TypeScript cop's own recommendation is "apply in Phase 3 alongside the other `.d.ts` edits," which is the correct phase when `index.d.ts` is being modified. |
