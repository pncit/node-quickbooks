## reviser — round 1

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| implementation-auditor-r1-f1 | Fixed | All 22 planning-artifact files (`plan.md`, `design.md`, `design-process-artifact.md`, `plan-process-artifact.md`, and 18 files under `review-design/` and `review-plan/`) were restored from the orphaned commit `04c4d8c` via `git checkout 04c4d8c -- docs/…` and committed to the branch as `b2aaba3`. `git ls-files docs/ | wc -l` = 24; `plan.md` and `design.md` verified present on disk and in the tracked tree. |
| implementation-auditor-r1-f2 | Fixed | The restore commit (`b2aaba3`) makes all planning-artifact content reachable from `feat/node-quickbooks-upstream-resync` without relying on the reflog. The orphaned commit `04c4d8c` itself remains an unreachable object (it was on a branch lineage that the hard reset skipped over, not a linear ancestor), but its entire doc tree is now tracked on the branch tip; a clone or `git gc` will not lose the planning record. Whether the historical planning commits also need a recovery ref is a judgment the plan does not settle — the content is intact, which is what f2 requires. |
| implementation-auditor-r1-f3 | Fixed | `implementation-phase1-notes.md` §5 now documents the unanticipated side effect (planning-doc destruction), the remediation applied (restore + commit), and the missing exit-gate assertion flagged for the Planner. Plan Adherence self-score corrected from 10 to 8.5 in §9; §10 updated accordingly. Changes committed in `74357e1`. |
| implementation-auditor-r1-f4 | Fixed | The missing artifact-survival assertion gap is now recorded in `implementation-phase1-notes.md` §5 ("Plan gap noted"), explicitly flagging that a gate of the form `test -f docs/…/plan.md` would have caught the destruction immediately and should be added if Phase 1 is ever re-run. Per the auditor's own guidance, re-running Phase 1 is not required since f1/f2 are remediated; recording the gap was the actionable item. |
