## reviser — round 1

I verified each finding directly against `upstream/master` (`2.0.49`) and a simulated install of the
post-reset + post-bump dependency tree before disposing of it. All five findings are well-grounded;
all are Fixed. Notable confirmations: `uuid@^8.3.2` is a *direct* upstream dep (declared in
`package.json`, used at `index.js:12`/`:2352` `uuid.v1()`) that survives the reset and retains
`GHSA-w5hq-g745-h8pq`; `require('uuid').v1()` still resolves under the bumped `uuid@14.0.0`; the
`mocha@10.1.0` devDep tree carries six unrelated advisories so a bare `npm audit` is never clean,
while `npm audit --omit=dev` on the bumped tree reports `0 vulnerabilities`.

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|------------------------------------------|
| plan-auditor-r1-f1 | Fixed | Confirmed `uuid` is a direct upstream dep surviving the reset and that `uuid@^8.3.2` keeps a moderate advisory; simulated install shows `>=11.1.1` (resolves `14.0.0`) clears it and `require('uuid').v1()` still works (the sole call site). Chose option (a) — add `uuid` as a third bump — because the design's success criterion is "all eight cleared / clean audit," which a bump satisfies without an `Accepted` divergence; re-scoping the gate to tolerate the residual (option b) would contradict the design. Edited: Phase 4 Goal/Step 1/Step 2/Step 4/example/Tests/Exit Gate now bump `uuid` to `>=11.1.1`, verify `npm ls uuid` and a runtime `uuid.v1()` smoke check, and note the semver-major rationale. Corrected the "removed with request" mis-attribution in Goals (uuid/form-data are direct, not via-request) and added a Repo-Context bullet documenting uuid's direct status, the advisory, and the v14 `v1()` verification. (The design's Success-Criteria *table* still labels uuid/form-data "via request"; that is a cosmetic design-doc wording issue outside the plan's edit scope — the plan now achieves the design's actual end-state, all eight cleared, so no escalation.) |
| plan-auditor-r1-f2 | Fixed | Confirmed the substring scan is unsound: against the bumped tree clean modules produce count `0` (so the plan's `grep && echo "STILL FLAGGED"` silently passes regardless), and the loop keys on raw name occurrence, not advisory presence, giving no signal on advisory IDs. Replaced it (Phase 4 Step 3 + example + Exit Gate) with a structural check: `npm audit --omit=dev --json` asserting `.metadata.vulnerabilities.total === 0`, plus a belt-and-suspenders `.vulnerabilities | keys` check that none of the eight named modules appears. Verified both pass on the simulated tree (`jq` present on host/CI; fallback to the `npm audit --omit=dev` exit code noted). |
| plan-auditor-r1-f3 | Fixed | Agreed the case-4 assertion (`callbackArgs[0] === null` in one `setImmediate`) did not prove the rejection was contained by the hook's *own* `.catch`. Rewrote the rejection-isolation case: registered a **mandatory** `process.on('unhandledRejection', …)` guard in `beforeEach`/`afterEach` and assert `unhandled.length === 0`; added `assert.strictEqual(hookSettled, true)`; and sequenced the final assertions off a short `setTimeout(…, 20)` (outlasting the microtask chain and the later `unhandledRejection` macrotask tick) instead of a single `setImmediate`. Updated Step 2 notes (now "not optional"), the Tests subsection, and the Exit Gate to match. |
| plan-auditor-r1-f4 | Fixed | Confirmed no `npm install` ran before Phase 4, so Phase 3's `npx mocha` gate (which `require`s `axios` and `../index`) would throw `MODULE_NOT_FOUND`. Added an explicit Phase 3 **Step 0** `npm install` over the inherited lockfile as the dependency-availability point, with the first module-load smoke test there; Phase 4 keeps its clean regen (`rm -rf node_modules package-lock.json && npm install`). Stated the dependency-availability point explicitly in Implementation Notes ("Phases 1-2 gate only on git/grep; node_modules first installed at start of Phase 3"), and moved Phase 1's deferred `node -e require` reference from "Phase 4" to "Phase 3." |
| plan-auditor-r1-f5 | Fixed | Confirmed `grep -c` returning `0` exits non-zero (breaks `&&` chains) and that unescaped `.`/`(` are regex metacharacters. Added a global "Gate-grep conventions" note in Implementation Notes mandating `grep -qF` for literal assertions and `[ "$(grep -c … )" = "0" ]` for count gates, and converted every `grep` exit-gate across Phases 1-4 (preconditions, reset checks, signature/shape checks, lockfile-absence checks) to that form. |
