## reviser — round 2

Addressed every Open finding from architect-r1 (f1–f6) and engineer-r1 (f1–f4). Verified the
load-bearing claims directly against `upstream/master` (`2.0.49`) and the fork `HEAD` before
disposition: upstream's constructor JSDoc stops at `@param minorversion` (f4 confirmed); upstream's
`refreshAccessToken` is HTTP-status-based `.then`/`.catch` with no body inspection, while the fork
`HEAD` is HTTP-body-based (`r.body.error === "invalid_grant"` → success vs `else`) (f1 confirmed).
Cross-checked the fuze-consumer-contract memory: `fuze` binds to the success-path rotation hook and
the rejection-isolation contract, not the failure-path error-arg shape — so f1's divergence is in
scope of the design's Non-Goals and is dispositioned as a scope/disclosure fix, not a behavior change.
Internal self-review found no additional issues; the edits are internally consistent (uuid pin,
string `minorversion`, and the logging-vs-empty-catch test are mutually compatible).

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|------------------------------------------|
| architect-r1-f1 | Fixed | Confirmed the divergence is real: fork `HEAD` `refreshAccessToken` classifies success on `r.body.error === "invalid_grant"`; upstream's axios path classifies on HTTP status (`.then`/`.catch`). The reset intentionally adopts upstream's status-based `.catch(err, err.response, …)` shape. Added (1) an Assumptions bullet scoping "identical observable behavior" to the success/rotation/hook path and stating the failure-path classification intentionally becomes upstream's; (2) a Phase-2 Step-2 scope note forbidding re-creation of the body-based branch; (3) a Deferred-Validation clause to confirm `fuze`'s token-refresh tests are indifferent to the changed failure-arg shape. No runtime behavior added — the failure path is upstream verbatim, consistent with the design's Non-Goal of not altering error propagation and the fuze memory (fuze observes only the success-path hook). |
| architect-r1-f2 | Fixed | The runtime calls `this.refreshTokenCallBack(this.refreshToken)` (one arg) but nothing pinned call arity to the `.d.ts` single-`token` signature. Extended test case 1 to capture `arguments.length` in the hook and assert `argCount === 1`, locking runtime arity to the declared `(token: string) => …` type so an implementor passing `(token, refreshResponse)` fails the gate. Added a matching sentence to the Phase-3 Step-2 notes. |
| architect-r1-f3 | Fixed | The "no third reconciliation point" check was presented as a gate but relied on eyeballing the request↔axios rewrite diff. Replaced it with a mechanical, falsifiable gate: assert `refreshTokenCallBack` and the rotation-guard string are fork-only tokens (count 0 upstream, present on `HEAD`) and live inside the two known regions — these are the feature's only behavioral markers, so the reset re-applies exactly them. Demoted the broad `git diff` skim to an explicitly advisory best-effort backstop (not a hard gate). Updated the Phase-1 example block and Step-2 accordingly. |
| architect-r1-f4 | Fixed | Added an Assumptions bullet: `npm test` remains credential-gated (inherited upstream property, not introduced here), so the de-facto unattended/CI gate is the isolated `npx mocha test/refreshTokenCallBack.test.js`; a future maintainer/`fuze` CI should not treat green `npm test` as required. Named the `test`/`test:unit` split as a deliberate deferred follow-up rather than a silently broken script. |
| architect-r1-f5 | Fixed | Changed the `uuid` bump from open-ended `">=11.1.1"` to caret-pinned `"^14.0.0"` (caret on the verified current major), consistent with the `^1.13.8`/`^5.9.2` pins and the design's "thin patch layer / pinned" posture. Updated all occurrences (Summary goal, Phase-4 goal/Step-1/Step-2 notes, example block, exit gate). The advisory is cleared either way (`14.0.0` ≫ the `>=11.1.1` fix); the caret bounds the float to the one major whose `v1()` call site was tested, avoiding a silent future `uuid@15+` pull. |
| architect-r1-f6 | Fixed | Added one sentence to Phase-3 Step-0 notes: the `fast-xml-parser@4.x` install/verify is deliberate sequencing — proving the re-applied feature loads and its test passes *before* Phase 4 layers the bumps, so a Phase-4 failure localizes to a bump — not redundant churn. |
| engineer-r1-f1 | Fixed | Changed the canonical Phase-2 `.catch` from a bare empty no-op to a `this.debug`-gated `console.log` of the failure, reusing the constructor's existing `debug` flag (the `.then` is `.bind(this)`, so `this.debug` is in scope). Swallow semantics unchanged (no rethrow, no await) so Decision 3 isolation holds; only observability is added — a hook rejection means a rotated refresh token was not persisted, which the operator must be able to see. Reworded the note so the logging form is the recommended shape and the empty catch is an acceptable fallback. Verified the Phase-3 test is agnostic to the catch body (it asserts `unhandled.length`/`callbackArgs`, never that the catch is empty), so both forms pass. |
| engineer-r1-f2 | Fixed | The `unhandledRejection` guard ran in all four cases but only case 4 asserted on it. Added `assert.strictEqual(unhandled.length, 0)` to the tails of cases 1-3 (deferring cases' final assertions into `setImmediate` where needed to observe the counter), turning "no path leaks an unhandled rejection" into a suite-wide property. Updated the narrative note. |
| engineer-r1-f3 | Fixed | Changed `makeClient`'s `minorversion` argument from the number `75` to the string `'75'` to match the re-applied `.d.ts` type `minorversion?: string | null` and upstream's `minorversion || 75` string default, removing the latent type mismatch in the same change-set; added an inline comment explaining the choice. |
| engineer-r1-f4 | Fixed | Verified upstream's constructor JSDoc stops at `@param minorversion` (documents neither `oauthversion` nor `refreshToken`). Updated Phase-2 Step-1 notes and the Documentation subsection to add all three missing `@param` lines (`oauthversion`, `refreshToken`, `refreshTokenCallBack`) in order, making the JSDoc contiguous through param 11 rather than skipping params 9-10. Noted this is inside the constructor region already edited (the feature's doc footprint), not an out-of-scope upstream tidy. |
