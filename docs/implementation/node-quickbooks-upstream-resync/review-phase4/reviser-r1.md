## reviser — round 1

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| architect-r1-f1 | Fixed | Down-pinned `uuid` from `^14.0.0` to `^11.1.1` in `package.json`. `uuid@11.1.1` ships a dual CJS/ESM build with a `require` conditional export that resolves to `./dist/cjs/index.js` (confirmed via `require.resolve('uuid')` after reinstall), making it safe under `require('uuid')` on all Node versions with conditional-exports support (Node ≥12), not just Node ≥20.19/22. `^11.1.1` is both the minimum fix boundary for GHSA-w5hq-g745-h8pq and the last major with a true CJS `require` export. `uuid.v1()` still resolves from the CJS build. `package-lock.json` regenerated; `npm audit --omit=dev` still reports `total: 0`. |
| architect-r1-f2 | Fixed | Moot by the f1 fix: `uuid@11.1.1` has a proper `require` conditional export and imposes no ESM-only runtime floor. No `engines` field is needed to guard against `ERR_REQUIRE_ESM` because the CJS load path is unconditionally available. Finding closed by down-pin. |
| engineer-r1-f1 | Fixed | Moot by the `uuid` down-pin to `^11.1.1`. Neither `uuid@11.1.1` nor `fast-xml-parser@5.9.2` declares an `engines` constraint, and neither imposes a practical floor above what conditional-exports support requires (Node ≥12). No ESM-only `require` floor was introduced. No `engines` field change is needed. |
| engineer-r1-f2 | Fixed | Updated `implementation-phase4-notes.md` §8 to accurately state that zero new *direct* dependencies were introduced but that `fast-xml-parser@5.x` adds the following new transitive packages to the production tree: `@nodable/entities`, `fast-xml-builder`, `is-unsafe`, `path-expression-matcher`, `strnum`, `xml-naming`, and `anynum` (transitive via `strnum`). The amended text notes that all are MIT-licensed, all are internal to the parser's 5.x implementation, and all are within the confirmed `total: 0` `--omit=dev` audit result. |
| project-lead-r1-f1 | Fixed | Corrected `repository.url` to `"https://github.com/pncit/node-quickbooks.git"`, `bugs.url` to `"https://github.com/pncit/node-quickbooks/issues"`, and `homepage` to `"https://github.com/pncit/node-quickbooks"` in `package.json`. Lockfile not regenerated (these fields are not embedded in `package-lock.json`). |
