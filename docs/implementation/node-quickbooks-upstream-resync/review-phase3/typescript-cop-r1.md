## typescript-cop — round 1

Phase 3 introduces two lines to `index.d.ts` (one constructor parameter and one instance field, both `refreshTokenCallBack?: (token: string) => void | Promise<void>`) and one new test file (`test/refreshTokenCallBack.test.js`). The diff against `upstream/master` confirms those are the only changes; every other line of `index.d.ts` is upstream verbatim and is not Phase 3's introduction. The review scope is therefore the two added type lines and the new test file.

**Type declarations (`index.d.ts`).** Both additions are correct. The hook type `(token: string) => void | Promise<void>` matches the runtime call site `this.refreshTokenCallBack(this.refreshToken)` (where `this.refreshToken` has just been assigned from `refreshResponse.refresh_token`, which is `string` per `RefreshTokenResponse`). The optional marker `?` on both the parameter and the field correctly admits the 10-argument construction form. The field is placed immediately after `refreshToken?: string | null` (mirroring the constructor parameter order and the runtime field order). No object-form overload was added (Decision 2 honored). No other declaration was altered.

**Test file (`test/refreshTokenCallBack.test.js`).** The test is structurally sound. The `axios.post` stub intercepts the actual call form used by `index.js` (property access on the module singleton, not a destructured copy). The microtask ordering analysis confirms `hookSettled` is set before the 20 ms `setTimeout` fires: `Promise.reject().catch(hookFn)` schedules `hookFn` as a microtask that runs and sets `hookSettled = true` before the isolation layer's `.catch` handler, and both complete long before the 20 ms macrotask fires. The three-way Decision 3 assertion (`callbackArgs[0] === null`, `hookSettled === true`, `unhandled.length === 0`) genuinely pins the isolation mechanism rather than merely observing that nothing threw. The `beforeEach`/`afterEach` lifecycle correctly registers and removes the unhandled-rejection listener with no leak between cases. No new devDependencies were introduced.

One low-severity observation is raised below; everything else is clean.

## Findings

| ID | Severity | Status | Category | Location | Finding | Recommendation |
|----|----------|--------|----------|----------|---------|----------------|
| typescript-cop-r1-f1 | Low | Open | TypeHole | `test/refreshTokenCallBack.test.js:127` | In case 4, `callbackArgs` is initialised to `null` and assigned only inside the `refreshAccessToken` callback (a microtask), yet the `setTimeout` handler dereferences it unconditionally as `callbackArgs[0]`. If the callback were ever not called (e.g. a future regression in the runtime code) the assertion throws `TypeError: Cannot read properties of null` rather than a meaningful assertion failure, making the failure mode harder to diagnose. | Add a guard before the property access: `assert.notStrictEqual(callbackArgs, null, 'refreshAccessToken callback was never called');` as the first statement inside the `setTimeout` handler, before `callbackArgs[0]`. This turns a potential `TypeError` crash into a named assertion failure with a clear message. |
